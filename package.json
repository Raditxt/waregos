{
  "name": "waregos",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "turbo": "latest",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "pino-pretty": "^13.1.3"
  }
}
